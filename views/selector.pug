extends layout

block content
  h1= title
  - var image_names=["Toyotablack.jpg","Toyotablue.jpg","Toyotared.jpg","Toyotawhite.jpg","Toyotayellow.jpg"]
   - for(let i=0; i<3; i++) {
        - var pic= Math.floor(Math.random()*5)
           img(src=`/images/${image_names[pic]}` alt=`${pic}` width='300px' height='300px')
   - }